name: app2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.2.0
  shared_preferences: ^2.1.1
  path_provider: ^2.0.14
  firebase_core: ^2.8.0
  firebase_auth: ^4.4.0
  cloud_firestore: ^4.7.0
  firebase_storage: ^11.0.0
  google_maps_flutter: ^2.4.0
  geolocator: ^9.0.2
  flutterfire_ui: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
